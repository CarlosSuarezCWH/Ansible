---
- name: Eliminar backups viejos reteniendo últimos 7
  hosts: all
  tasks:
    - name: Eliminar viejos
      shell: find /var/opt/gitlab/backups/ -name "*_gitlab_backup.tar" -mtime +7 -exec rm -f {} \;
      changed_when: true
