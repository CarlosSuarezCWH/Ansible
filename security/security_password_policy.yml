---
- name: Aplicar política de contraseñas robusta
  hosts: all
  ignore_unreachable: yes
  tasks:
    - name: Update login.defs (ejemplo)
      lineinfile:
        path: /etc/login.defs
        regexp: '^PASS_MAX_DAYS'
        line: 'PASS_MAX_DAYS   90'
