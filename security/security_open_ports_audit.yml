---
- name: Listar puertos abiertos y justificar uso
  hosts: all
  ignore_unreachable: yes
  tasks:
    - name: Nmap
      command: ss -tulpn
      register: ports
      changed_when: false
    - name: Mostrar
      debug:
        var: ports.stdout_lines
