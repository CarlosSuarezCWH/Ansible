---
- name: Configurar protección /wp-admin, xmlrpc
  hosts: all
  ignore_unreachable: yes
  tasks:
    - name: Asegurar wo stack install fail2ban
      command: wo stack install --fail2ban
      changed_when: true
