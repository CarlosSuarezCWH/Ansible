---
- name: Estado de servicios WordOps
  hosts: all
  tasks:
    - name: Check services
      command: systemctl is-active {{ item }}
      register: wo_services
      loop:
        - nginx
        - php8.1-fpm
        - mysql
        - redis-server
      failed_when: false
      changed_when: false

    - name: Report
      debug:
        msg: |
          {% for r in wo_services.results %}
          - {{ r.item }}: {{ r.stdout | default('failed') }}
          {% endfor %}
