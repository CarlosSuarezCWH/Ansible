---
- name: Generar reporte consolidado (Mock)
  hosts: localhost
  ignore_unreachable: yes
  tasks:
    - name: Este playbook unifica otros usando roles o imports
      debug:
        msg: "Reporte unificado requiere ejecucion local y plantillas jinja. Archivo placeholder."
