---
- name: Rotar y comprimir logs viejos
  hosts: all
  ignore_unreachable: yes
  become: true
  tasks:
    - name: Forzar logrotate
      command: logrotate -f /etc/logrotate.conf
      changed_when: true
