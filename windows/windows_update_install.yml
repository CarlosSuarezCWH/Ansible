---
- name: Instalar actualizaciones de seguridad
  hosts: all
  ignore_unreachable: yes
  tasks:
    - name: Install updates
      win_updates:
        category_names:
          - SecurityUpdates
        state: installed
